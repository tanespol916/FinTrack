# Nixpacks configuration for FinTrack monorepo
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "npm ci --include=dev",
  "npx prisma generate", 
  "npm run build"
]

[start]
cmd = "npm start"