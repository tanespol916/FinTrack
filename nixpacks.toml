# Nixpacks configuration for FinTrack monorepo
[phases.setup]
nixPkgs = ["nodejs_20", "postgresql"]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
PORT = "4000"
