# Nixpacks configuration for Discord Bot
[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmds = [
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = ["python", "bot.py"]

[variables]
PYTHONUNBUFFERED = "1"
